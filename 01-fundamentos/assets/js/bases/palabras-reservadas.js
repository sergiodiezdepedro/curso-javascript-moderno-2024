// ? 'const' es una palabra reservada, por lo que crea un error de sintaxis
// ? let const = 123

// ? 'enum' es otra palabra reservada
// ? let enum = 678;

// ? No se puede nombrar una variable empezando con un número, pero sí se pueden utilizar después (let a123 es válido)
// ? let 8abc = 100;

// ? lowerCamelCase para el nombre de las variables
// ? UpperCamelCase para el nombre de las clases
// ? No usar caracteres especiales (ñ, ẞ, o ç, por ejemplo), ni espacios
// ? Se puede usar el guión bajo para separar números (99_99)
// ? Se puede usar el símbolo dolar
// ? Se puede empezar el nombre de una variable con un guión bajo (_variable)
